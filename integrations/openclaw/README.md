# OpenClaw integration

Conceptual gateway for exposing a small set of typed research actions to a
local agent. The intended boundary excludes raw shell, SSH, credentials,
arbitrary files, arbitrary network access, and direct Slurm commands.
