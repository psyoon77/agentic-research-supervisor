# Contracts

Proposed versioned interfaces for experiment intent, capability snapshots,
exact allocations, checkpoints, telemetry, incidents, repairs, and reports.

Contracts should reject unknown required fields and preserve source, runtime,
resource, parameter, and evidence identity across every transition.
