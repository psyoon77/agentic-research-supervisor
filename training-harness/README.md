# Training harness

Planned program-owned boundary for validating an experiment contract, launching
one declared workload, emitting structured progress, writing compatible
checkpoints, and returning a machine-readable result.

The supervisor should never invent missing entrypoints, parameters, metrics, or
resource requirements on behalf of this layer.
